/*%@ return data.list.map(function(r) {
    return {
        fileName: r + '.txt', context: r };
    })
%*/
/*%@
return data.list.map(function(r) {
    return {
        fileName: r + fileName, context: r + context };
    }) %*/
/*%= context %*/
